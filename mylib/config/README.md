# config 模块文档

---

## 需要实现的功能

**[x]** - `必须配置列表`

1. 定义 **运行时必须配置** -> `List` 访问

**[ ]** - `自动发现模式`

1. 以 `必须配置列表` 为黑名单, 发现 `TOML` 文件中的配置项
2. 自动将配置项注册为 **实例属性** -> `Dict` 访问
3. 自动建立 `额外配置索引` -> `List` 访问

**[ ]** - `获取实例属性`

1. 默认获取 `所有实例属性` -> `List` 访问
2. 支持 **黑白名单** -> `List` 定义: `必须配置列表` `额外配置索引`

**[ ]** - `重构show_summary`

1. 保留现有打印逻辑
2. 新增打印 `额外配置索引`
