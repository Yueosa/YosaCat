# 脚本使用说明

---

## **`systemd_deploy.sh`**

```shell
# 部署或更新服务
sudo bash scripts/systemd_deploy.sh --deploy

# 重启服务
sudo bash scripts/systemd_deploy.sh --restart

# 卸载服务
sudo bash scripts/systemd_deploy.sh --remove
```

